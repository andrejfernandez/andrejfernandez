// Dependencies
import styled from "styled-components";

const Projects = () => {
  return (
    <StyledProjects>
      <h1>Projects</h1>
    </StyledProjects>
  );
};

const StyledProjects = styled.div``;

export default Projects;
